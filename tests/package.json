{
  "name": "pb-dashboard-tests",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Browser regression tests for the PB dashboard (verify driver-scoping, etc.)",
  "scripts": {
    "test:verify": "node verify-driver-scope.spec.mjs"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
